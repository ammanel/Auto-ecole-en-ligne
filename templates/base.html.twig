<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta content="Premium Multipurpose Admin & Dashboard Template" name="description" />
        <meta content="Themesbrand" name="author" />
         <!-- App favicon -->
        <link rel="shortcut icon" href="{{asset('SuperAdmin/assets/images/favicon.ico')}}">

        <!-- plugin css -->
        <link href="{{asset('SuperAdmin/assets/libs/admin-resources/jquery.vectormap/jquery-jvectormap-1.2.2.css')}}" rel="stylesheet" type="text/css" />

        <!-- preloader css -->
        <link rel="stylesheet" href="{{asset('SuperAdmin/assets/css/preloader.min.css')}}" type="text/css" />

        <!-- Bootstrap Css -->
        <link href="{{asset('SuperAdmin/assets/css/bootstrap.min.css')}}" id="bootstrap-style" rel="stylesheet" type="text/css" />
        <!-- Icons Css -->
        <link href="{{asset('SuperAdmin/assets/css/icons.min.css')}}" rel="stylesheet" type="text/css" />
        <!-- App Css-->
        <link href="{{asset('SuperAdmin/assets/css/app.min.css')}}" id="app-style" rel="stylesheet" type="text/css" />
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        {% block body %}
        
                 <!-- Begin page -->
        <div id="layout-wrapper">

            {% block header %}

                  {{ include('template_super_admin/header.html.twig') }}
            
            {% endblock %}



            {% block menu %}

            <div class="vertical-menu">

                 {{ include('template_super_admin/menu.html.twig') }}
            </div>
            <!-- Left Sidebar End -->

            {% endblock %}

            {% block titre %}
             <div class="main-content">

                <div class="page-content">
                    <div class="container-fluid">

                        <!-- start page title -->
                        <div class="row">
                            <div class="col-12">
                                <div class="page-title-box d-sm-flex align-items-center justify-content-between">
                                    <h4 class="mb-sm-0 font-size-18">Dashboard</h4>

                                    <div class="page-title-right">
                                        <ol class="breadcrumb m-0">
                                            <li class="breadcrumb-item"><a href="javascript: void(0);">Dashboard</a></li>
                                            <li class="breadcrumb-item active">Dashboard</li>
                                        </ol>
                                    </div>

                                </div>
                            </div>
                        </div>
                        <!-- end page title -->

            {% endblock %}

            {% block contenu %}

            {% endblock %}
        
        {% endblock %}

        <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
        <script>
            function notifier2(idrecupar){
                
            
                    
                    
                    
            
                    
            }


            

            $('body a.ecole').click(function(event){
                event.preventDefault();
                const like = this.href;
                axios.get(like).then(function(response){
                console.log(response.data[0].id);
                Nom = document.getElementById("Nom").value;
                heure = document.getElementById("heure").value;
                idconnecter = document.getElementById("idconnecter").value;
                
                var contenu = "Vous avez un cour pratique de "+heure+" avec Monsieur/Madame "+Nom
                axios.post("apprenant/messages?contenu="+contenu+"&idconnecter="+idconnecter+"&idrecupar="+response.data[0].id).then(function(response) {
        
                    const messages = response.data;
                })

                
            })
            });
    
        </script>
    </body>
</html>
